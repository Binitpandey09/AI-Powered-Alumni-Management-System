from django.db import models

# Models will be implemented on Day 2
# Planned models:
# - Resume (user, file, parsed_text, score, feedback, created_at)
# - MockInterview (user, job_role, questions, answers, feedback, score, created_at)
# - SkillGapAnalysis (user, current_skills, target_role, gaps, recommendations, created_at)
